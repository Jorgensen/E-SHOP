extends layout

block content
  .container
    .row
      .col-md-6
        img.detail(src="#{phone.url}")
      .col-md-6
        h1 #{phone.name}
        p 
          small #{phone.description}
        p 价格：#{phone.price}
        p 运费：#{phone.ship}
